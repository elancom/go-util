package sign

import (
	"github.com/elancom/go-util/str"
)

func sign(key string, data map[string]any) string {
	// 移除关键字
	if data["sign"] != nil || data["_sign"] != nil {

	}

	// 过滤排序

	// 连接字符串

	// 连接秘钥

	// 摘要

	return str.ToUpper("")
}

func check() bool {
	return false
}
